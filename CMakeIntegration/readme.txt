Now this project is fun.
It is both; QtDesigner project and Eclipse CDT project.
QtDesigner opens and builds this project as usual.
Eclipse is configured to build using CMake (right click->Make Targets->Build...->cmake). Then just click "Build" hammer.